# Secrets — never commit
*passwords*.txt
*.pem
*.key
client.keys
secrets*
wazuh-install-files.tar
.env
*.env
migration-export/

# Migration working notes -- not for readers, see migration/ for the committed writeup
VM Migration/

# OS junk
.DS_Store
Thumbs.db

# Large media (keep screenshots, ignore raw captures/video)
*.mov
*.mp4
